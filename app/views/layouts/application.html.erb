<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Sustainable Outdoor Maintenance Jobs" %></title>
    <%= stylesheet_link_tag 'application', params[:controller], media: 'all' %>
    <%= javascript_include_tag 'application', params[:controller] %>
    <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyASzLV9HksurXgWeTPy_LpfNv-2c61vSuM&callback=initMap"></script>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if current_user %>
      <div class="top-bar">
        <div class="top-bar-left">
          <ul class="dropdown menu" data-dropdown-menu>
            <li class="menu-text">
              <%= link_to 'Sustainable Jobs', dashboard_path %>
              <ul class="menu vertical">
                <% if current_user.is_homeowner %>
                  <li class="menu">
                    <%= link_to 'Create Job Posting', new_job_posting_path %>
                  </li>
                  <li class="menu">
                    <%= link_to 'Your Job Postings', job_postings_path %>
                  </li>
                  <li class="menu">
                    <%= link_to 'Request(s) to do your Jobs', requests_job_postings_path %>
                  </li>
                <% else %>
                  <li class="menu">
                    <%= link_to 'Your Job Request', job_requests_path %>
                  </li>
                <% end %>
              </ul>
            </li>
          </ul>
        </div>
        <div class="top-bar-right">
          <ul class="dropdown menu" data-dropdown-menu>
            <li class="menu-text">
              <%= link_to current_user.name, user_path(current_user) %>
              <ul class="menu vertical">
                <li><%= link_to 'Edit Profile', edit_user_registration_path %></li>
                <li><%= link_to 'Logout', destroy_user_session_path, :method => :delete %></li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    <% end %>
    <% if notice.present? %>
      <p id="notice"><%= notice.html_safe %></p>
    <% end %>
    <%= yield %></body></html>
